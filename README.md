# How to run

## For Rust

* Install Rust
* Go to the puzzle directory
* Run:
  ```sh
  cargo run
  ```
* Launch tests:
  ```sh
  cargo test
  ```

## For Zig

* Install Zig
* Go to the puzzle directory
* Run:
  ```sh
  zig run main.zig
  ```
* Launch tests:
  ```sh
  zig test main.zig
  ```
